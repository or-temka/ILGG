export interface AppsProps {
  classNames?: {
    wrapper?: string
  }
}

export interface ItemInfoProps {
  classNames?: {
    wrapper?: string
  }
}

export interface ItemsFieldProps {
  classNames?: {
    wrapper?: string
  }
}
